# 项目协作约定

本文件适用于整个仓库。无论由人还是 AI 修改，都要把“代码、测试、记录”视为同一次变更。

## 开始修改前

1. 阅读 `PROJECT_STATUS.md`，确认当前状态、进行中事项和已知风险。
2. 阅读 `CHANGELOG.md` 的 `[未发布]` 部分，避免重复实现或覆盖尚未完成的工作。
3. 涉及产品口径、数据结构、兼容性、安全或架构取舍时，先检查 `docs/DECISIONS.md`。
4. 检查 `git status --short`。不得擅自覆盖或删除已有未提交改动。

## 修改完成前

1. 在 `CHANGELOG.md` 的 `[未发布]` 下记录面向使用者的变化；纯格式调整可省略。
2. 更新 `PROJECT_STATUS.md`：完成的事项移出“进行中”，新增风险或后续工作写入对应区域。
3. 若产生了影响后续实现的取舍，在 `docs/DECISIONS.md` 追加一条决策，不得悄悄改变既有口径。
4. 新行为必须有测试，修复缺陷必须尽量增加回归测试。
5. 运行与改动相关的测试，并在交付说明中写明结果和未验证项。
6. 最后再次检查 `git diff` 和 `git status --short`，确保没有临时文件、意外文件或遗漏记录。

## 记录标准

- 记录结果和影响，不写逐行操作流水。
- 一条变更应能回答：改了什么、为什么、影响哪里、如何验证。
- `PROJECT_STATUS.md` 只保留当前真相；历史事实放在 `CHANGELOG.md`。
- `docs/DECISIONS.md` 记录“为什么这样做”和替代方案，不重复代码细节。
- 不把“稍后处理”留在聊天记录里；必须写入 `PROJECT_STATUS.md` 的“下一步/待处理”。
