# 电商评论持续追踪系统

持续追踪模式把一次性输出升级为一个长期维护的项目工作簿。每个项目可以包含多个商品，但评论只会在同一个 `project_id + item_key` 内比较和去重；相同型号的不同商品 ID 不会串库。

## 创建项目

```powershell
python app.py init-project `
  --workbook ".\projects\ScreenBar_消费者证据库.xlsx" `
  --project-id "screenbar_geo_2026" `
  --name "ScreenBar 与竞品消费者反馈追踪" `
  --objective "每半月更新商品评论与问答"
```

项目工作簿会创建以下管理 Sheet：

- `project_info`、`products`、`snapshots`
- `content_master`、`analysis`、`snapshot_contents`
- `update_log`、`review_queue`

同时会生成兼容现有复核台的 `content_all`、`ai_candidates` 和汇总 Sheet。

## 先检查更新，不写入

```powershell
python app.py update `
  --workbook ".\projects\ScreenBar_消费者证据库.xlsx" `
  --project-id "screenbar_geo_2026" `
  --file ".\input_html\mindduo2_20260717.html" ".\input_html\halo2_20260717.html" `
  --dry-run
```

商品 ID 会从每个 SingleFile 页面中自动提取，不需要用户输入。输出会逐文件显示页面商品 ID、是否首次导入、最近快照、预计新增数、重复数、待确认数和重叠边界状态。页面中提取不到商品 ID 时，该文件会明确报错且不会写入。

## 确认并写入

去掉 `--dry-run` 即可执行更新：

```powershell
python app.py update `
  --workbook ".\projects\ScreenBar_消费者证据库.xlsx" `
  --project-id "screenbar_geo_2026" `
  --file ".\input_html\mindduo2_20260717.html" ".\input_html\halo2_20260717.html" `
  --platform "tmall" `
  --brand-product-id "benq_mindduo2"
```

可选参数：

- `--capture-time "2026-07-17 23:09:32"`：覆盖文件修改时间。
- `--allow-backfill`：允许导入早于最新快照的历史页面，不覆盖最新状态。
- `--brand`、`--model-name`：补充商品聚合信息。
- `--no-sentiment`：本次更新不运行情绪判断；默认开启。
- `--sentiment-detail-limit 50`：限制进入 LLM 详析的高价值新增评论数。

`--file` 后可以提供一个或多个页面。系统按给定顺序逐个更新，因此同一商品的后续页面会以上一个页面为历史基准。相同文件再次导入时，`content_master` 不变，但 `update_log` 会记录一次 warning。

## 重新分析，不重新解析 HTML

```powershell
python app.py reanalyze `
  --workbook ".\projects\ScreenBar_消费者证据库.xlsx" `
  --project-id "screenbar_geo_2026" `
  --module "ai_candidate" `
  --version "ai_rules_v1.4"
```

`reanalyze` 当前重算本地 AI 候选规则。情绪判断在每次正式增量更新时对新增评论执行，并单独保存版本与运行状态。

## 补判历史评论情绪

旧项目中已经存在、但尚未完成情绪判断的评论和追评，可执行：

```powershell
python app.py backfill-sentiment `
  --workbook ".\projects\ScreenBar_消费者证据库.xlsx" `
  --project-id "screenbar_geo_2026" `
  --dry-run
```

预览会显示待处理数和需要发送给 LLM 的数量，不会调用模型。默认策略是全量 LLM，因此正式执行时每条尚未判别的评论和追评都会进入 LLM 快判。确认后去掉 `--dry-run`：

```powershell
python app.py backfill-sentiment `
  --workbook ".\projects\ScreenBar_消费者证据库.xlsx" `
  --project-id "screenbar_geo_2026"
```

补判只处理没有有效情绪结果的评论，重复执行会自动跳过已完成内容。正式执行会把待处理评论文本发送给配置的 LLM 服务，并在写回前备份工作簿。

## 启动持续追踪 UI

```powershell
.\start_ui.ps1
```

也可以直接运行：

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py
```

不建议直接调用系统全局的 `streamlit` 命令。若 8501 端口已经被旧进程占用，应先关闭旧终端中的 Streamlit，再重新运行上述命令。

持续追踪 UI 分成两个清楚的入口：

- **品牌看板**：默认入口，给品牌侧用户只读查看评论规模、问大家、情绪分类、AI 影响 A–D 分级、评论原文和商品对比。
- **维护后台**：给项目维护者创建项目、上传页面、执行更新、检查 fallback、日志、失败和待复核记录。

商品 ID 全部从页面自动读取。旧版一次性 Excel 复核台仍保留，可用以下命令单独启动：

```powershell
streamlit run review_app.py
```

### UI 使用流程

#### 品牌侧：只看结果

启动 UI 后默认进入“品牌看板”。品牌侧用户不负责上传页面或更新数据，只需要选择项目并查看：

- **品牌总览**：商品评论总数、问大家问题与回答数、积极/消极/褒贬混合/中性/未判别评论数，以及 AI 影响 A–D 各级评论数。
- **销售进展**：按主评论日期查看各商品的月度评论数，通过 1%–30% 的预计评论率滑块，用“评论数 ÷ 预计评论率”估算当月销量和环比变化，默认评论率为 5%。商品名称会注明天猫、淘宝或京东来源；追评不会重复计为销售，缺少日期的评论不进入估算；结果不是平台真实订单数据。
- **评论洞察**：按商品、情绪、AI 等级和关键词筛选评论，查看评论原文、表扬点、抱怨点并下载当前结果。
- **购前评论**：单独查看消费者购买前的比较、选择、顾虑和决策评论。
- **AI 影响**：查看逐月“评论/购前/AI 候选”三色趋势、A–D 分级定义、各级数量和相应评论证据。
- **月份汇总**：按月比较评论量、购前决策、AI 候选和 A–D 等级。
- **高价值评论**：查看经过 LLM 详析的关键证据、判断原因和内容价值。
- **商品对比**：比较不同商品的评论量、问大家、情绪分布和 AI 相关情况。
- **看板说明**：查看各页面和“未判别”的含义。

“未判别”表示评论还没有完成情绪分析，不代表中性。如果所有评论都显示未判别，需要维护者对历史评论安排补充分析；后续新增评论会在正式更新时默认执行情绪判断。

#### 维护者：选择或创建项目

在左侧把“使用身份”切换到“维护后台”。“维护项目”会自动列出 `projects` 目录中的持续追踪工作簿。

- 选择已有工作簿即可维护项目。
- 选择“手动输入路径”可以打开其他位置的项目工作簿。
- 输入的工作簿尚不存在时，页面会显示创建项目表单。
- 创建空项目只建立 Excel 结构，不解析页面，也不会调用 LLM。

#### 维护者：查看项目状态

“项目状态”显示商品数、累计内容、AI 候选、待复核数、最近快照、Fallback 快照和情绪判别覆盖情况。品牌侧用户不需要处理这些维护指标。

#### 维护者：批量上传并预览

进入“数据更新”：

1. 平台通常保持“自动识别”。
2. 可选填写型号聚合 ID。
3. 默认保持“启用 LLM 情绪判断”。
4. “情绪判断策略”默认保持“全量 LLM”。
5. 选择一个或多个 SingleFile 页面。
6. 点击“检查更新”。

预览阶段会按文件顺序模拟增量更新，但不会调用 LLM，也不会修改正式工作簿。预览表会逐文件显示：

- 页面自动提取的商品 ID
- 是否首次导入
- 最近历史快照
- 提取、新增、已存在和待确认数量
- 边界状态与连续重叠长度
- 本次新增评论中的情绪判断目标数量

页面缺少商品 ID 时，该文件会明确报错。

#### 维护者：确认写入和情绪判断

点击“确认写入”后，系统才会：

1. 把新增原始内容写入临时工作簿。
2. 把本次新增的每条评论和追评提交给已配置的 DeepSeek 做情绪快判。
3. 对少量高价值评论运行 LLM 详析。
4. 写入 `sentiment_fast`、`sentiment_detail`、失败、耗时和运行摘要 Sheet。
5. 校验工作簿、备份旧文件并原子替换主文件。

情绪判断默认开启，策略默认为“全量 LLM”。维护者可以改选“本地规则优先（节省调用）”，但这不是默认方式。情绪判断不是原始数据持久化的前提；如果模型配置缺失、网络失败或部分结果不合法，原始内容仍会安全保存，本次更新会显示 warning，并把具体失败写入 `sentiment_failures` 和 `sentiment_run_summary`。

#### 维护者：检查数据质量

- “数据质量与日志”：检查 fallback 数量、每次抓取的边界状态、增量数量、情绪运行状态和错误信息。
- “待复核队列”：查看文本相同但身份信息不足等低置信记录。
- “扩展分析”：查看情绪快判、情绪详析和已有 LLM 结构化分析 Sheet。

### LLM 情绪配置

默认读取：

```text
config/llm_providers.local.json
config/secrets/deepseek_api_key.txt
```

创建项目时不会调用 LLM。只有在“批量更新”页面点击“确认写入”，且本次存在新增商品评论时才会调用。已存在内容不会重复调用模型。

## Excel 写入安全

每次正式更新都会先写入同目录临时文件并校验必需 Sheet，然后把旧工作簿复制到 `backups/`，最后原子替换主文件。解析或写入异常不会覆盖原工作簿。

## 运行测试

```powershell
python -m pytest -q
```
