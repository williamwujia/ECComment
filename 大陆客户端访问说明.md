# 大陆客户端访问说明

这个版本面向中文客户演示和复核使用：界面文案已中文化，数据处理和复核台均在本机运行，不依赖外部网站或大模型 API。

## 1. 安装依赖

如果大陆网络访问 PyPI 较慢，优先使用国内镜像安装：

```powershell
.\install_deps_cn.ps1
```

也可以继续使用原始命令：

```powershell
python -m pip install -r requirements.txt
```

## 2. 启动中文复核台

如只在本机使用：

```powershell
.\start_ui.ps1
```

如需要同一局域网内的客户机访问：

```powershell
.\start_ui_cn.ps1
```

启动后终端会显示两个地址：

- 本机访问地址：`http://localhost:8501`
- 客户端访问地址：`http://主机IP:8501`

客户机需要与运行脚本的电脑处在同一局域网，并在浏览器打开“客户端访问地址”。如果无法访问，请检查 Windows 防火墙是否允许 Python/Streamlit 使用 8501 端口。

## 3. 使用流程

1. 先运行 `python main.py` 生成 `output/*.xlsx`。
2. 打开中文复核台后，直接选择 `output` 目录里的结果文件，或上传脚本输出的 Excel。
3. 重点查看“数据总览”“购前决策评论”“AI 候选证据库”“复核记录”。
4. 人工复核结果会保存到 `output/review_feedback.csv`。

## 4. 交付注意

- 前端只读取 Excel，不重新解析 HTML。
- 工具不会登录电商平台，不会联网抓取页面，不会调用大模型 API。
- 下载的 CSV 保留内部字段名，便于后续脚本继续读取；页面表格已提供中文列名。
