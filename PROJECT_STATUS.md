# 项目状态（唯一当前入口）

最后核对：2026-08-24
当前分支：`codex/continuous-tracking-v2`

> 开始任何修改前先读本页。这里描述“现在是什么样”，历史变化见 `CHANGELOG.md`，重要取舍见 `docs/DECISIONS.md`。

## 当前目标

为淘宝、天猫、京东评论提供本地采集、增量去重、AI 影响识别、情感分析、质量检查与持续追踪，并保证每次更新可预览、可审计、可回退。

## 当前能力基线

- 持续追踪项目使用独立 Excel 工作簿，按项目和商品隔离数据。
- 支持淘宝/天猫 SingleFile 页面以及淘宝、天猫、京东结构化评论 CSV 导入。
- 提供独立“电商评论更新助手”Chrome 扩展，支持三平台混合队列、登录暂停与恢复、本地检查点和统一 CSV 下载。
- 综合助手可复用用户正常打开的当前商品页，绕过天猫/淘宝裸链接偶发返回的无评论简化页，并支持确认评论卡片后受控滚动内嵌评论列表。
- 原有京东专用 Chrome 采集器保持独立，可继续作为京东采集兜底。
- 支持增量去重、快照历史、安全写入与错误快照回退。
- 支持 AI 影响 A–D 分级、情感快判、详细分析和人工复核。
- 提供品牌看板、维护后台、质量日志和部署/上传安全限制。
- LLM Flash 模型名称已规范化，情感分析支持并发处理。
- 品牌看板提供“销售进展”，按主评论日期汇总月度评论数，并以固定 5% 评论率估算销量及环比变化。
- 销售进展中的商品名称带有天猫、淘宝或京东平台标识。
- 侧栏会提醒超过 14 天未更新或尚无快照的 SKU。
- Windows 启动器会在默认端口 8501 被占用时自动递增到下一个可用端口。

## 进行中（尚未提交）

- 当前没有已登记的未完成实现。

## 已知风险与待核对

- README 在部分 Windows PowerShell 输出中可能显示乱码；修改前应明确使用 UTF-8 读取和写入，避免破坏原文件编码。
- 当前大量能力集中在 `tracker_ui.py`，后续修改容易产生耦合和遗漏；新增独立业务逻辑应优先放入 `ui/` 或领域模块，并由 UI 调用。
- 仓库 `.venv` 已可运行完整测试；2026-08-24 验证结果为 Python `108 passed, 19 subtests passed`，综合助手与京东扩展 Node 测试共 2 项通过。
- 综合助手的队列、恢复及 CSV 导入已由自动测试覆盖；现场已确认天猫裸链接可能返回无评论简化页。当前页通道已在商品 `1021070862839` 上完成 20 条真实闭环验证：时间排序、受控滚动、UTF-8 下载、日期/SKU/正文和无重复均通过。自动直达模式仍需更多商品验证，暂不得表述为稳定无人值守采集。

## 下一步/待处理

- 若后续能获得平台真实销量，可校准或替换当前固定 5% 评论率假设；现阶段估算结果不得表述为平台真实订单。
- 确认 14 天是固定产品规则还是应改为项目级可配置项。
- 再选择 1–2 个淘宝/天猫商品验证自动直达或当前页通道，并对属于现有项目的商品完成服务器预览和正式写入；测试商品 `1021070862839` 不在本地现有项目中，不应误提交。
- 每次发布时，把 `CHANGELOG.md` 的 `[未发布]` 内容归入带日期版本。

## 常用验证

```powershell
python -m pytest -q
git diff --check
git status --short
```

## 记录导航

- 当前状态与下一步：本文件
- 变更历史：`CHANGELOG.md`
- 决策与取舍：`docs/DECISIONS.md`
- 记录工作方式：`docs/CHANGE_PROCESS.md`
