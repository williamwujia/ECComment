# 项目状态（唯一当前入口）

最后核对：2026-08-24
当前分支：`codex/continuous-tracking-v2`

> 开始任何修改前先读本页。这里描述“现在是什么样”，历史变化见 `CHANGELOG.md`，重要取舍见 `docs/DECISIONS.md`。

## 当前目标

为淘宝、天猫、京东评论提供本地采集、增量去重、AI 影响识别、情感分析、质量检查与持续追踪，并保证每次更新可预览、可审计、可回退。

## 当前能力基线

- 持续追踪项目使用独立 Excel 工作簿，按项目和商品隔离数据。
- 支持淘宝/天猫 SingleFile 页面以及京东评论 CSV 导入。
- 支持增量去重、快照历史、安全写入与错误快照回退。
- 支持 AI 影响 A–D 分级、情感快判、详细分析和人工复核。
- 提供品牌看板、维护后台、质量日志和部署/上传安全限制。
- LLM Flash 模型名称已规范化，情感分析支持并发处理。
- 品牌看板提供“销售进展”，按主评论日期汇总月度评论数，并通过 1%–30% 评论率滑块估算销量及环比变化；默认值为 5%。
- 销售进展中的商品名称带有天猫、淘宝或京东平台标识。
- 侧栏会提醒超过 14 天未更新或尚无快照的 SKU，并在待更新明细中标明所属电商平台。
- Windows 启动器会在默认端口 8501 被占用时自动递增到下一个可用端口。
- 京东 Chrome 扩展会同时采集评论正文与评论图片，任务结束后生成字段兼容的 ECComment CSV，以及包含 `images.csv + images/ + README.txt` 的 ZIP 图片包；图片下载失败不会阻断文本结果。

## 进行中（尚未提交）

- 当前没有已登记的未完成实现。

## 已知风险与待核对

- 工作区存在名为 `NUL` 的异常未跟踪项；在 Windows 上可能是保留设备名，需要确认来源后再处理。
- README 在部分 Windows PowerShell 输出中可能显示乱码；修改前应明确使用 UTF-8 读取和写入，避免破坏原文件编码。
- 当前大量能力集中在 `tracker_ui.py`，后续修改容易产生耦合和遗漏；新增独立业务逻辑应优先放入 `ui/` 或领域模块，并由 UI 调用。
- 当前可调用的打包 Python 环境缺少 `pytest`、`bs4`、`streamlit` 和 `plotly`；销售估算、可调评论率、平台标识、更新提醒和既有指标共 14 个定点测试已直接执行通过，完整测试仍需在安装开发依赖的环境复核。
- 京东评论图片已用 DOM/URL/ZIP 自动化测试验证，但尚需在真实京东评论弹层上抽样核对不同商品模板的图片节点及 CDN 地址；未成功下载的图片会保留在 ZIP 的 `images.csv` 中并标明原因。

## 下一步/待处理

- 若后续能获得平台真实销量，可校准评论率滑块的默认值或推荐范围；现阶段估算结果不得表述为平台真实订单。
- 在安装开发依赖的环境运行完整测试；Windows 受限环境可使用仓库内隔离临时目录并关闭缓存插件：`python -m pytest -q -p no:cacheprovider --basetemp .codex-test-tmp`。
- 确认 14 天是固定产品规则还是应改为项目级可配置项。
- 查明并安全处理异常 `NUL` 项。
- 用至少一个含图京东商品做端到端采集，核对 ZIP 中图片数、图片清晰度和 `images.csv` 评论对应关系。

## 常用验证

```powershell
python -m pytest -q
git diff --check
git status --short
```

## 记录导航

- 当前状态与下一步：本文件
- 变更历史：`CHANGELOG.md`
- 决策与取舍：`docs/DECISIONS.md`
- 记录工作方式：`docs/CHANGE_PROCESS.md`
