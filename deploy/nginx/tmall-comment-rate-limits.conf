# Place this file in /etc/nginx/conf.d/. It is included from nginx's http {}
# block on the standard Debian/Ubuntu Nginx layout.
limit_req_zone $binary_remote_addr zone=tmall_comment_request:10m rate=20r/m;
limit_conn_zone $binary_remote_addr zone=tmall_comment_connection:10m;
